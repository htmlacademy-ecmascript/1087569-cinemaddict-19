const COMMENT_EMOTIONS = ['smile', 'sleeping', 'puke', 'angry'];

const Keys = {
  ESCAPE: 'Escape',
  ESC: 'Esc'
};

const FilterType = {
  ALL: 'all movies',
  WATCHLIST: 'watchlist',
  HISTORY: 'history',
  FAVORITES: 'favorites'
};

export { COMMENT_EMOTIONS, Keys, FilterType };
